/**
 * @template T
 * @param {T[]} items 
 * @return {T[]}
 */
function makeUnique(items) {
	// implement...
	return items;
}
/**
 * @template T
 * @param {T[]} items 
 * @param {(item: T) => any} keyExtractor 
 */
function uniqueByKey(items, keyExtractor) {
	// implement...
	return items;
}